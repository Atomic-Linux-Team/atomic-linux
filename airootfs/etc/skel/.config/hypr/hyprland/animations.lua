-- hyprland.animations.lua
-- Animation settings for Atomic Linux ISO

-- Simple fade in/out for windows
animation = {
    "windows, 1, 3, default, slide"
}

-- Workspace transition animation
animation = {
    "workspace, 0.4, 0.2, default, slide"
}
