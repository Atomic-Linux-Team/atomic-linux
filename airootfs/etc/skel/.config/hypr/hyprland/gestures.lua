-- hyprland.gestures.lua
-- Basic gesture bindings for workspace switching

-- Swipe left/right with three fingers to change workspace
bind = "GESTURE_SWIPE, 3, LEFT, workspace, -1"
bind = "GESTURE_SWIPE, 3, RIGHT, workspace, +1"
