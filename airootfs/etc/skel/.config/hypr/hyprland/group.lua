-- hyprland.group.lua
-- Window grouping settings for Atomic Linux ISO

-- Enable grouping of windows (Alt+Tab, etc.)
group = {
    enabled = true,
    threshold = 10,  -- distance threshold for grouping
}
